<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="android:Theme.Material.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:colorPrimary">@color/brown</item>
        <!-- Secondary brand color. -->
        <item name="android:colorSecondary">@color/coffee</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/brown</item>
        <!-- Customize your theme here. -->
    </style>
</resources>